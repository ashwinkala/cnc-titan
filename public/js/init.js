(function($){
    $(function(){
      // for the side navigation
      $('.button-collapse').sideNav();
      // for the drop down form
      $('select').material_select();
      }); // end of document ready
  })(jQuery); // end of jQuery name space